Phase 3 step 2 of docs/plan.md: two variables. X COV Y, X CORR Y (Pearson), X REGRESS Y (least squares by domino: with a vector X, the intercept and slope; with a matrix X, one column per regressor, the intercept and a coefficient per column), X RESID Y, X RSQ Y, and B PREDICT X (the fitted values for new X from coefficients B). Sample extended with a data set whose fit is exact (a line) and one with scatter whose numbers are worked by hand; pinned in both modes. HOWREGRESS. Docs updated.