Phase 0 of docs/plan.md, the rest of the scaffold. Already in place at setup: COPYRIGHT, LICENSE, .gitattributes, .gitignore, README, docs/plan.md, CLAUDE.md, /mw-cp, the justfile, scripts/sw-apl.sh (which binary runs), scripts/check-provenance.sh, scripts/check-ws.sh, scripts/lib-shim.sh, scripts/reg.sh, scripts/reg-seed.sh, scripts/run-samples.sh, scripts/gen-changes.sh.

Add: scripts/gen-index.sh writing ws/library.json (name EXTENDED; one entry per workspace file with name, file, modes read from the modes line; the field names are the plan's proposal until sw-apl's browser-libraries step settles them) and a `just index` recipe plus a gate that the tracked index is current; a GitHub Pages workflow publishing ws/ (Access-Control-Allow-Origin is Pages' default); samples/README.md (what a sample is, how one names its mode on its first line, that every workspace has one); tests/reg-rs/ with its .gitkeep or first seeded test; work/README.md (scratch, untracked, where converted material belongs); docs/workspaces.md (the conventions in the plan's "Conventions every workspace follows", as a reference: DESCRIBE, HOWNAME, prompts, STOP, delta-prefixed subfunctions, 64 columns, uppercase, no quad-LX in a both-mode file, the modes rule and the pair rule), docs/testing.md (transcripts are the tests; reg-rs; the gates; the binary; the shim; how a sample names its mode), docs/saga.md (a heading for this saga), docs/citations.md (the APL\360 User's Manual, the 5110 APL Reference Manual, the APLSV user's guide, the Try MTS page, the CHM APL collection page for STATPAK's manual, each as a source described and never copied), docs/library.md (how sw-apl reaches this library at the CLI, the service and the browser: the shim today, --lib 2= and sw-apl.toml when sw-apl's library-config lands, library.json and Pages for the browser; the table of what this repository needs from sw-apl).

TDD here is the gates: `just gates` and `just check` pass on the empty library, `just index` is idempotent, and `scripts/reg.sh run` runs with zero tests. README's documentation list already names every doc; make each exist. Commit, push, report.
