Phase 3 step 3 of docs/plan.md: tests and generation. M TTEST1 V (one sample against a mean M: the statistic and degrees of freedom), X TTEST2 Y (two samples, pooled variance), CHISQ M (a contingency table: the statistic and degrees of freedom), NORMAL Z (the standard normal distribution function by a rational approximation good to six places, its source cited in docs/citations.md), TDIST if a short approximation exists so a p-value can be printed; RANDN N (normal deviates by Box-Muller from ? and ○), RANDU N (uniform on 0 to 1, and A RANDU on an interval), N SAMPLE V (deal without replacement), SHUFFLE V. Transcripts pinned in both modes, the random ones reproducible from the file's link. Milestone 3. docs/workspaces.md, samples/README.md, docs/citations.md, README table row present tense, docs/saga.md.