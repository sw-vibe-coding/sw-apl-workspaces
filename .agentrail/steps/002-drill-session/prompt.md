Phase 2 step 2 of docs/plan.md: the drill itself. EASY (one function, scalars), DRILL (one or two functions, vectors), HARD (two or three functions with parentheses and a reduction); a topic menu at the start read with quote-quad -- ARITHMETIC, COMPARE, MAXMIN, REDUCE, ALL -- with the tables filtered by topic; the loop: print the expression, read the answer with quad, mark with tolerant equality of value and shape (a scalar for a one-element vector accepted with a remark), show the right answer when wrong, show the expression worked out after three wrong in a row, keep score, STOP ends and prints the score. DESCRIBE and HOWDRILL. Samples per level with scripted answers, right and wrong, in both modes; every printed line within 64 columns. Docs updated.