Phase 2 step 4 of docs/plan.md: the (B) pair. WRITE, a drill that shows a value and asks the reader to write an expression that produces it, evaluated with execute and marked; it cannot be in the both-mode file, so DRILL becomes DRILL.a-70.apl.ws (the tables drill, modes (A)) beside DRILL.b-75.apl.ws (the same functions unchanged plus WRITE and ⎕LX←'DESCRIBE', modes (B)), both called DRILL. A gate, scripts/check-pair.sh, diffs the functions the two files share and fails when they differ; it joins just gates. The (A) samples move to the .a-70 file's behaviour unchanged; a (B) sample plays WRITE with scripted answers. Docs: workspaces.md's pair rule shown on DRILL, README table row, samples/README.