Phase 1 step 6 of docs/plan.md: COURSE lessons 15 and 16 -- Workspaces; The two modes -- as ∆L15 and ∆L16 with their quizzes, then the read-through. Workspaces: )SAVE, )LOAD, )COPY, )CLEAR, )DROP, library 0 and the numbered libraries, DESCRIBE as the convention, what a saved file cannot hold (a suspended function). The two modes: what (A) has alone (I-beams, )ORIGIN )DIGITS )WIDTH, groups) and what (B) has alone (execute, format, the quad names), the same small program in each, and where next: DRILL, STATS, sw-apl's docs. Neither lesson may use a one-mode feature itself; it shows them as text.

Then: a sample per mode that runs every lesson and every quiz from START to the end with NEXT (pauses answered, every quiz with a right answer), pinned; the final lesson list recorded in docs/workspaces.md and CONTENTS; the README table row for COURSE stated in the present tense; samples/README.md complete; docs/saga.md's course entry written. Milestone 1: )LOAD 1 COURSE (through the shim), START, and every lesson and quiz runs in (A) and in (B).
