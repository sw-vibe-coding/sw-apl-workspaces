Phase 2 step 3 of docs/plan.md: INDEX and STRUCTURE topics -- bracket indexing, take, drop, reverse, rotate, compress, membership, index-of -- with operands chosen so every expression is valid (indices within range, conformable compressions), and a vector reduction in HARD; the tables, the dispatch and ∆NEW extended; the samples extended. Milestone 2: DRILL runs in both modes and the transcripts are pinned. Docs updated.