Phase 2 step 1 of docs/plan.md. ws/DRILL.apl.ws, modes (A)(B), SOURCE line, LINK 16807: the monadic and dyadic glyph tables (character vectors), ∆MON (the monadic function whose glyph is F, on X, by a branch table) and ∆DYA (the dyadic one between the caller's ∆X and ∆Y, read by dynamic scope as sw-apl's BIRDS reads FN), ∆CHARS (a whole number or vector as APL prints it, without format, high minus included), ∆NEW (choose a function and operands that keep the answer whole where the function allows -- division on multiples, power small, residue with a divisor of 1 to 5, logic on 1 and 0 -- and set the caller's ∆E, the expression's characters, and ∆V, its value), and SHOW N, which prints N generated expressions each followed by its value. DESCRIBE says what DRILL will be and that SHOW is here now. Sample drill-show.apl (A) loads DRILL, DESCRIBE, SHOW 100; the (B) twin adds VERIFY, defined in the sample with execute, which generates a hundred more and counts how many displays evaluate to the dispatched value, printing the count: the check that display and value agree. TDD: the transcript first. docs/workspaces.md gains DRILL's entry; samples/README.md its paragraph. Commit, push, report.