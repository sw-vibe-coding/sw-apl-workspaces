Phase 1 step 5 of docs/plan.md: COURSE lessons 11 to 14 -- Defining functions; Branching; Input and output; Errors -- as ∆L11 .. ∆L14 with their quizzes. These lessons ask the reader to type del definitions at the prompt, so the lesson text shows a definition line by line in the form the del editor echoes, and the quiz asks for the value a defined function would give rather than reading a definition. Branching: the conditional branch idiom, labels, loops, why a reduction is usually better. Input and output: quad and quote-quad, prompts, a function that asks and checks -- which is what QUIZ itself does, so the lesson may point at ∆ASK as a worked example. Errors: reading a report, the six common errors, the state indicator, )SI, a bare branch to clear, )SIV. A sample may leave a function suspended on purpose and clear it. Samples samples/course-11-14.apl and its (B) twin, transcripts first; `just check` in both modes; docs updated.
