Phase 1 step 2 of docs/plan.md: COURSE's shape, and lesson 1 in full.

ws/COURSE.apl.ws, modes (A)(B), SOURCE line, LINK 16807. DESCRIBE (what COURSE is, START, CONTENTS, LESSON N, NEXT, QUIZ N, STOP at any prompt); CONTENTS listing the sixteen lessons by number and title from the plan; LESSON N dispatching to a function per lesson (∆L1 .. ∆L16; the unwritten ones print "NOT YET WRITTEN" and their title); START is LESSON 1; NEXT reads the variable LAST that LESSON sets and goes on; a section pause ∆PAUSE printing a prompt with quote-quad and reading a line, STOP leaving the lesson by a branch; QUIZ N with ∆ASK (print the question, read the answer with quad, STOP leaves) and ∆CHECK (tolerant equality of value and shape; a scalar for a one-element vector accepted and remarked on; the right answer shown when wrong); a score at the end of a quiz.

Lesson 1, "Where you are", in full: the prompt, system commands begin with ), )DIALECT, )WSID, )LIBS, )LIB, )HELP, how a lesson works, STOP and NEXT, then QUIZ 1 with three or four questions on what was read. Every printed line at most 64 characters; uppercase. The lesson shows what )DIALECT and )LIBS print as the plan expects; the lib-course-opening step re-checks the text against sw-apl later.

Sample samples/course-1.apl (mode (A)) and samples/course-1-75.apl (first line ⍝!MODES (B)): )LOAD 1 COURSE through the shim, DESCRIBE, CONTENTS, START with the pauses answered, QUIZ 1 with a right answer, a wrong one, and STOP; then LESSON 2 showing NOT YET WRITTEN. Write the expected transcript by hand before the APL, seed the reg-rs tests, and pin. `just check` passes in both modes. docs/workspaces.md gains COURSE's entry; samples/README.md its paragraph.
