Phase 5 step lib-cli of docs/plan.md, moved up (owner direction 2026-09-24) because sw-apl's library-config landed (sw-apl commit a718f19: --lib N=DIR[,NAME], sw-apl.toml [[library]] tables, )LIBS, and the browser reading ws/library.json). Replace the shim: scripts/reg-seed.sh and scripts/run-samples.sh run sw-apl with --lib 2=ws,EXTENDED; scripts/lib-shim.sh and the shim recipe go; the samples say )LOAD 2 COURSE and their comments no longer mention the shim; every baseline is rebased with that one reason. Docs in the present tense: README (Use it, Building and testing), docs/library.md (the flag and the TOML file as they are, the browser as it is, the needs table), docs/testing.md (the binary must be sw-apl a718f19 or later; no shim), docs/workspaces.md if it mentions the shim, CLAUDE.md, samples/README.md, and docs/plan.md (Phase 0 and Phase 5 updated; the needs table). Gates, check, reg; commit, push, report.